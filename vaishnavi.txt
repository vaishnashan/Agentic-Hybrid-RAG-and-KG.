# 5. Run the API
uvicorn utils.api7.main:app --reload --port 8000

# 6. Run the UI (separate terminal, API must be running)
streamlit run ui/app.py




.\venv\Scripts\activate

python -m utils.ingestion1.embed_and_index  

python -m utils.retrieval2.reranker

python -m utils.knowledge_graph3.neo4j_client       # sanity check: just verifies connection
python -m utils.knowledge_graph3.extractor          # builds the graph (Papers + Concepts)
python -m utils.knowledge_graph3.test_queries

python -m utils.agent4.graph_definition

python -m utils.observability5.tracing
streamlit run utils/observability5/dashboard_metrics.py

python -m utils.evaluation6.run_golden_set
python -m utils.evaluation6.run_ragas        # generates a RAGAS report
python -m utils.evaluation6.regression_check   # checks it against the threshold



streamlit run utils/ui8/app.py



Component	Change needed for deployment?
Chroma (chroma_db/)	None — just copy the folder into the image
BM25 (bm25_index.pkl)	None — just copy the file into the image
Embedding + reranker models	Pre-download during Docker build (small fix)
Neo4j	Move to Aura Free (cloud) — the one real change

agentic-rag-kg/
│
├── README.md
├── .env.example
├── .gitignore
├── docker-compose.yml
├── Dockerfile
├── requirements.txt / pyproject.toml
│
├── data/
│   ├── raw/                     # original corpus files (pdfs, txt, etc.)
│   ├── processed/                # cleaned + chunked docs
│   └── golden_eval_set.json      # your 30-40 test questions + expected answers
│
├── src/
│   │
│   ├── ingestion/                 # Week 1
│   │   ├── loader.py              # reads raw corpus
│   │   ├── chunker.py             # chunking strategy (semantic/fixed)
│   │   ├── embed_and_index.py     # embeds + loads into Qdrant/Chroma
│   │   └── bm25_index.py          # sparse index build
│   │
│   ├── knowledge_graph/           # Week 1
│   │   ├── schema.py              # entity/relation types
│   │   ├── extractor.py           # LLM/NLP entity-relation extraction
│   │   ├── neo4j_client.py        # connection + Cypher queries
│   │   └── graph_algorithms.py    # PageRank, community detection (Neo4j GDS)
│   │
│   ├── retrieval/                 # Week 1
│   │   ├── dense_retriever.py
│   │   ├── sparse_retriever.py
│   │   ├── hybrid_merge.py        # combine dense + sparse
│   │   └── reranker.py            # cross-encoder reranking
│   │
│   ├── agent/                     # Week 2
│   │   ├── graph_definition.py    # LangGraph nodes + edges wiring
│   │   ├── planner.py             # decomposes multi-hop questions
│   │   ├── router.py              # vector vs graph vs both
│   │   ├── reasoner.py            # combines retrieved context → draft answer
│   │   ├── self_critic.py         # confidence check + retry logic
│   │   ├── fallback.py            # graceful degradation paths
│   │   └── schemas.py             # Pydantic structured I/O per node
│   │
│   ├── resilience/                # Week 2
│   │   ├── circuit_breaker.py
│   │   ├── retry_policy.py
│   │   └── cache.py               # Redis semantic cache
│   │
│   ├── guardrails/                # Week 2
│   │   └── input_validation.py    # prompt-injection checks, length/format
│   │
│   ├── evaluation/                # Week 3
│   │   ├── run_ragas.py
│   │   ├── regression_check.py    # fails if metrics drop below threshold
│   │   └── reports/                # generated eval + perf reports
│   │
│   ├── observability/              # Week 3
│   │   ├── tracing.py              # Langfuse wiring
│   │   └── dashboard_metrics.py    # feeds monitoring dashboard
│   │
│   ├── api/                        # Week 4
│   │   ├── main.py                 # FastAPI app entrypoint
│   │   ├── routes/
│   │   │   ├── ask.py
│   │   │   ├── health.py
│   │   │   └── metrics.py
│   │   ├── auth.py                 # API key auth
│   │   └── rate_limit.py           # slowapi config
│   │
│   └── ui/                         # Week 4
│       └── app.py                  # Streamlit/Gradio UI + dashboard panel
│
├── tests/
│   ├── unit/
│   │   ├── test_retrieval.py
│   │   ├── test_agent_nodes.py
│   │   └── test_api.py
│   └── load/
│       └── locustfile.py           # Week 3 load testing
│
├── .github/
│   └── workflows/
│       └── ci_cd.yml                # lint → test → eval-gate → build → deploy
│
└── docs/
    ├── architecture_diagram.png     # (or .md with the flow you already have)
    └── resilience_design.md