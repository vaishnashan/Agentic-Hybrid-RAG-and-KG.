# --- Storage: load + chunk (utils/storage/loader.py, chunker.py) ---
pdfplumber
langchain
langchain-text-splitters
langchain-community

# --- Storage: dense + sparse index (utils/storage/dense_index.py, bm25_index.py) ---
# sentence-transformers is used at BUILD time (local embedding) and by
# retrieval/reranker.py (cross-encoder) — NOT for query-time embeddings,
# which call a cloud API instead (utils/retrieval/query_embedder.py).
sentence-transformers
chromadb
rank_bm25

# --- Storage: knowledge graph (utils/storage/graph_index.py, neo4j_client.py) ---
neo4j
networkx

# --- Agent orchestration (Week 2) ---
langgraph
pydantic
requests
python-dotenv
tenacity
pybreaker

# --- Evaluation (Week 3) ---
datasets
ragas
langchain-groq
langchain-huggingface

# --- Observability (Week 3) ---
langfuse
pandas

# --- API (Week 4) ---
fastapi
uvicorn[standard]
slowapi

# --- UI (Week 4) ---
streamlit

# --- Load testing (Week 3) ---
locust