# Backend URL, without a trailing slash.
API_BASE_URL=http://localhost:8000

# Must match the backend API key when authentication is enabled.
API_KEY=

# Optional. Render normally provides PORT automatically.
PORT=8501
