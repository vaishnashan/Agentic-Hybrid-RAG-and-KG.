# UI-only dependencies — matches exactly what ui/app.py actually imports.
# Deliberately NOT the backend's requirements.txt: that file has torch,
# chromadb, neo4j, langgraph, sentence-transformers, etc. — none of which
# this Streamlit UI touches, since it only talks to the API over HTTP.
streamlit
requests
python-dotenv
